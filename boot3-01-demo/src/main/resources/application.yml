# 1. k: v # k v之间是空格区分
# 2. 属性有层级关系，使用下一行，空两格空格
# 3. 左侧对齐的代表同一层级的属性

server:
  port: 9999

spring:
  servlet:
    multipart:
      max-file-size: 10MB
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    url: aaa